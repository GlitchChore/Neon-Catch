using UnityEngine;

namespace NeonCatch
{
    // Markiert eine kletterbare Leiter. Die eigentliche Kletter-Logik läuft im
    // PlayerController (OnTriggerEnter/Exit + W/S-Bewegung entlang der Leiter).
    public class Ladder : MonoBehaviour
    {
    }
}
